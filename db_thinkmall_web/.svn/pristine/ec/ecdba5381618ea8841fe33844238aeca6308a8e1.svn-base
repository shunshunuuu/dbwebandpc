 /**
  * 活动引导页2
  * @author wanliang
  */
define(function(require, exports, module){ 
	var _pageId = "#active_guidePagesTwo "; // 当前页面ID
	var appUtils = require("appUtils"); // 核心工具类

	/*
	 * 初始化
	 */
	function init(){
		
	}
	
	
	/*
	 * 绑定事件
	 */
	function bindPageEvent(){
		
		// 马上注册
		appUtils.bindEvent(_pageId + ".rig_btn", function(){
			appUtils.pageInit("active/guidePagesTwo", "main", {});
		});
		
	}
	
	/*
	 * 页面销毁
	 */
	function destroy(){
		
	}
	
	var guidePagesTwo = {
		"init": init,
		"bindPageEvent": bindPageEvent,
		"destroy": destroy
	};
	// 暴露对外的接口
	module.exports = guidePagesTwo;
});